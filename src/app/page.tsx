"use client"

import { MenuLayout } from "@/components/menu-layout"

export default function HomePage() {
  return <MenuLayout>{/* Home page content can be empty as we're showing the main menu */}</MenuLayout>
}
